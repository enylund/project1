<% if user_signed_in? %>
  <% if @test.blank? %>
    <%= render :partial => '/follow_channels/follow' %>
  <% else %>
    <%= render :partial => '/follow_channels/unfollow' %>
  <% end %>
<% end %>
<%= render @channel %>
