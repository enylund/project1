  $('#channel-name').find('#new_follow_channel').remove();
  $('#channel-name').prepend("<%= escape_javascript(render 'unfollow') %>");

  var fakeFlash = "<div id='flash_notice'> You are now following: <%= j(@channel.name) %> </div>";
  $('body').append(fakeFlash).find('#flash_notice').delay(1000).fadeOut(300, function() { 
	
	$(this).remove(); 
	
	});
